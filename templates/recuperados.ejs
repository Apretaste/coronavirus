<div class="row">
	<div class="col s12">
		<ul class="tabs tabs-fixed-width">
			<li class="tab"><a href="#" onclick="apretaste.send({command: 'CORONAVIRUS'})">Resumen</a></li>
			<li class="tab"><a href="#" onclick="apretaste.send({command: 'CORONAVIRUS ENFERMOS'})"><i class="material-icons">add_alert</i></a></li> 
			<li class="tab"><a href="#" onclick="apretaste.send({command: 'CORONAVIRUS MUERTES'})"><i class="material-icons">airline_seat_flat</i></a></li> 
			<li class="tab"><a href="#" class="active"><i class="material-icons">assignment_turned_in</i></a></li>
		</ul>

		<p>Número de personas en cada país que se ha recuperado del coronavirus.</p>

		<div class="input-field">
			<input placeholder="Escriba país para filtrar" type="text" onkeyup="filter(this.value)">
		</div>

		<ul class="collection">
			<% Object.keys(items).forEach(function(key){ %>
				<li class="collection-item avatar" data-value="<%= items[key].countryName %>">
					<img class="circle" src="{{APP_RESOURCES}}/images/flags/<%= items[key].countryCode %>.png"/>
					<span class="title"><%= items[key].countryName %></span>
					<p class="number"><%= items[key].total %></p>
				</li>
			<% }) %>
		</ul>
	</div>
</div>